<?xml version="1.0" encoding="utf-8"?>

<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Warna saat aktif (floating hint) -->
    <item android:state_focused="true" android:color="@color/grey_bold"/>
    <!-- Warna saat tidak aktif -->
    <item android:color="@color/grey_bold"/>
</selector>
